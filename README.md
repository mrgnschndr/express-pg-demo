# Self-Guided Lesson: Creating GET, POST, UPDATE, and DELETE Routes in Express with PostgreSQL

## Setting Up the Project

Run `npm i`

## To Test

Run `npm run test`

## To Start Server

Run `npm run start`

## To Setup DEV Database

Run:

`node ./db/setupDatabase.js`
`node ./db/seedDatabase.js`